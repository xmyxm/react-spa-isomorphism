import { weather } from './base/weather'
import { tripMenu } from './base/tripmenu'
import { tripDetail } from './base/tripdetail'
import { IndexModel } from './base/modelType'

export const models: IndexModel = { weather, tripMenu, tripDetail }
